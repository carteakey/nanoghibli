# TODO

## High Priority
- [ ] Implement VEO model integration for smoother animation transitions between stylized frames.
- [ ] Add comprehensive error handling for API rate limits and network timeouts.
- [ ] Allow batch processing of multiple videos or photo directories in one run.

## Medium Priority
- [ ] Add a progress bar to the frame extraction phase for large videos.
- [ ] Expose Gemini model parameters (like temperature) via CLI arguments for advanced tweaking.
- [ ] Optimize memory usage when processing high-resolution input files.

## Low Priority / Polish
- [ ] Add unit tests for `extractor.py` and `animator.py`.
- [ ] Support generating GIFs alongside MP4 outputs.
- [ ] Create a dedicated configuration file (e.g., `config.yaml`) for default settings.
